---
import type { OrganizationProfileProps, OrganizationSwitcherProps, Without } from '@appypeeps/clerk-types';

type Props = Without<OrganizationSwitcherProps, 'organizationProfileProps'> & {
  organizationProfileProps?: Pick<OrganizationProfileProps, 'appearance'>;
};

import InternalUIComponentRenderer from '../InternalUIComponentRenderer.astro';
---

<InternalUIComponentRenderer
  {...Astro.props}
  component='organization-switcher'
/>
<slot />
